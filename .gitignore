# Local tooling / editor state — not part of the deployed site
.claude/
.hallmark/
.wrangler/

# Scratch, backups, and raw render artifacts — kept on disk locally,
# never shipped. See docs/ for the actual project plans/specs.
work/
assets/vid_backup_orig/
assets/vid_backup_pre_upscale/
assets/vid_upscaled/
images/orig_backup/

# Unused source asset — not referenced by any page, kept locally only
assets/Alpine.mp4

# Raw source images / backups dropped at repo root while updating branding —
# the deployed copies live under images/ and logo.jpg; these are originals only
/alpine-aviation-badge.jpg
/image.jpg
/logo.jpg.bak

# OS junk
.DS_Store
Thumbs.db

# Frame-sequence detour — superseded by the restored video scroll, kept locally only
assets/frames/

# Optimized-encode working folder — kept locally, deployed copies live in assets/vid/
assets/vid_opt/
